# HAHABEST Mobile App

Мобильное приложение HAHABEST на Flutter для Android.

## Структура проекта

```
lib/
├── config/          # Конфигурация (API endpoints)
├── models/          # Модели данных
├── screens/         # Экраны приложения
├── services/        # Сервисы (API, Storage)
└── main.dart        # Точка входа
```

## Настройка

### 1. Установка зависимостей

```bash
flutter pub get
```

### 2. Настройка API URL

В файле `lib/config/api_config.dart` настройте базовый URL API:

- **Для Android эмулятора**: `http://10.0.2.2:8000` (уже настроено)
- **Для реального устройства**: `http://<IP_вашего_компьютера>:8000`
- **Для продакшена**: укажите URL вашего сервера

### 3. Запуск приложения

```bash
flutter run
```

## Функционал

### ✅ Реализовано

- **Страница входа**:
  - Валидация email и пароля
  - Показ/скрытие пароля
  - Обработка ошибок
  - Сохранение токенов
  - Красивый современный дизайн

- **Авторизация**:
  - Автоматическая проверка токенов при запуске
  - Обновление токенов при истечении
  - Выход из аккаунта

- **Профиль**:
  - Отображение данных пользователя
  - Базовая информация

### 🚧 В разработке

- Регистрация
- Google OAuth
- Полный функционал профиля
- Список вакансий
- Детали вакансии
- Резюме

## API

Приложение использует тот же бэкенд, что и веб-версия:
- Base URL: `http://localhost:8000` (или из конфига)
- Endpoints: `/api/auth/login`, `/api/users/me`, и т.д.

## Зависимости

- `http` - HTTP клиент
- `shared_preferences` - Локальное хранилище
- `email_validator` - Валидация email
- `provider` - Управление состоянием (для будущего использования)

## Разработка

Приложение создано с использованием:
- Flutter SDK 3.7.2+
- Material Design 3
- Современный UI/UX
